pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // 添加 Xposed API 仓库
        maven { url 'https://api.xposed.info/' }
    }
}

rootProject.name = "WeChatFriendFilter"
include ':app' 